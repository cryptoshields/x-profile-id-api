# nixpacks.toml – this works 100% on Railway in Nov 2025
[phases.setup]
  nixPkgs = ["python312", "chromium"]

[phases.build]
  cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
  cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT}"
